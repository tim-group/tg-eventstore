pluginManagement {
    repositories {
        maven {
            name "nexus"
            url "${repoUrl}/groups/public"
        }
    }
}

rootProject.name = "tg-eventstore"

include "api"
include "api-legacy"
include "memory"
include "mysql"
